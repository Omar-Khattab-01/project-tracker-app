import { index, integer, sqliteTable, text } from 'drizzle-orm/sqlite-core';
export const projects=sqliteTable('projects',{id:integer('id').primaryKey(),name:text('name').notNull(),description:text('description').notNull().default(''),priority:text('priority').notNull().default('Normal'),startDate:text('start_date').notNull(),targetEndDate:text('target_end_date').notNull(),notes:text('notes').notNull().default(''),archived:integer('archived',{mode:'boolean'}).notNull().default(false),createdAt:text('created_at').notNull().default(''),updatedAt:text('updated_at').notNull().default('')},t=>[index('idx_projects_archived').on(t.archived)]);
export const tasks=sqliteTable('tasks',{id:integer('id').primaryKey(),projectId:integer('project_id').notNull().references(()=>projects.id,{onDelete:'cascade'}),name:text('name').notNull(),type:text('type').notNull().default('Task'),owner:text('owner').notNull().default(''),startDate:text('start_date'),dueDate:text('due_date'),percentComplete:integer('percent_complete').notNull().default(0),priority:text('priority').notNull().default('Normal'),manualStatusOverride:text('manual_status_override'),notes:text('notes').notNull().default(''),sortOrder:integer('sort_order').notNull().default(0),archived:integer('archived',{mode:'boolean'}).notNull().default(false),predecessor:integer('predecessor'),createdAt:text('created_at').notNull().default(''),updatedAt:text('updated_at').notNull().default('')},t=>[index('idx_tasks_project_sort').on(t.projectId,t.sortOrder),index('idx_tasks_due_date').on(t.dueDate),index('idx_tasks_archived').on(t.archived)]);
